extends ../layout

block content
  div(class="container")
    div(class="row")

      h2 #{issue.journal.name}
      h3 Volume ##{issue.volume_number}, Issue ##{issue.issue_number}
      h4 Articles
      ul
        each article in issue.articles
          li
            a(href="/article/" + article.id) #{article.name}
